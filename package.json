{
  "name": "pi-hub-models",
  "version": "0.1.0",
  "description": "Pi TUI overlay: add a provider with multiple APIs to models.json",
  "keywords": [
    "pi-package",
    "pi-extension"
  ],
  "license": "MIT",
  "type": "module",
  "files": [
    "src",
    "README.md",
    "docs/SPEC.md"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/palmtom316/pi-hub-models.git"
  },
  "bugs": {
    "url": "https://github.com/palmtom316/pi-hub-models/issues"
  },
  "homepage": "https://github.com/palmtom316/pi-hub-models#readme",
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "scripts": {
    "test": "node --experimental-strip-types --test tests/*.test.ts"
  },
  "engines": {
    "node": ">=22"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.84.2",
    "@earendil-works/pi-tui": "^0.84.2"
  }
}
